# Authorized frozen-prediction calibration reporting supplement

Owner permission: the user replied「允許」to the explicit request to read only previously sealed 2025 predictions to fill the108 missing Overall calibration plots, without raw-data access, retraining or new inference. This narrowly supersedes the earlier no-new2025-access boundary for this derived reporting step only.

Allowed patient-level source: `outputs/v2/accepted/p1_06/evaluation/predictions.parquet`, SHA256 `3118c1555e4ed7c1fec3b7043c7c702c88a2ac242fa4bf8f9a72d5226d21db9f`. Read only existing external2025 probability/label/task/row/model-binding columns. Log each projection read. The accepted artifact manifest, its model/task bindings and already-public aggregates may be read for identity checks. Do not read raw source data or `sealed_cohort.parquet`; do not load models, call predict/fit, change selection/calibration/thresholds, or alter accepted evidence.

Reporting recipe: mirror the existing specialty reliability-bin display: stable ascending probability ordering and ten nearly equal-count bins (or fewer only if fewer than ten rows); calculate bin count, event count, mean stored probability and observed event fraction. Bins depend on predictions/order, not labels. Preserve ties using original frozen-row order and disclose that ties can span adjacent bins. These are descriptive reliability points, not a fitted calibration mapping or new confidence bands. No calibration intercept/slope fitting or model comparison/selection is part of this task.

Deliver to existing v3 Plotly/HTML interface and source-backed aggregate downloads. Chart question: how stored Overall model probabilities compare with observed 2025 frequencies. Use fixed0–1 axes, a neutral identity line, labelled model/outcome/split and bin denominators. Preserve the existing v1/v2-style interface per the owner's prior instruction; no replacement dashboard framework. Native Plotly line+markers is the selected site renderer. All108 model/outcome cells must be accounted for, but no numerical success or adequacy claim is implied by coverage.

Main owns production and site integration. Independent reviewer may make one separately logged projection read of this same source to recompute bins, exact task/model/row identity and counts; no wider data access. File-based handoffs only: side workers must not message parent/main without separate owner approval.
