# 2024 共同樣本評估

本次更新讓主比較的同一族群、同一結局之 12 個模型使用相同病人紀錄。涵蓋 9 個結局 × 3 個族群 × 12 個模型，共 324 格。

## 評估名單

先固定既有 Overall rSI-sMS 可計算的 7,289 筆 2024 紀錄，再與原 OP_NAME 分科名單取交集：內科 4,579 筆、外科 2,688 筆；其餘 22 筆只留在 Overall。內外科樣本分別減少 191、24 筆。這是共同可評分子集，不是 OHCA 排除族群。

同一族群、同一結局內，所有模型的紀錄與標籤逐筆一致。三個族群仍是不同的人群；不能用各族群整體分數，宣稱某族群專屬模型勝過 Overall 模型的配對比較。

## 本次重新計算

198 格分科非 rSI-sMS 模型重新計算 AUROC、AUPRC（average precision）、Brier、既有閾值下的混淆矩陣與敏感度／特異度／PPV／NPV、ROC／PR 曲線、校準診斷及 DCA。其餘 126 格原本已使用相同名單，沿用既有結果。

信賴區間及校準帶沿用既有 `run_stage2a_v2.evaluate_pair`：2,000 次 outcome-stratified patient-group cluster bootstrap，基礎 seed 20260710，25 個校準曲線網格點。沿用原函式的數值可估計性規則，不以本次結果選擇新模型、閾值或校準方法。校準截距／斜率是報告用診斷，不會修改輸出的機率。

ROC／PR 圖最多顯示 401 個原始曲線點，點估計仍使用全部共同样本的既有預測機率。DCA 展示 0.01–0.99 的風險門檻，不改變已鎖定的操作閾值。各模型的原操作閾值保持不變，不保證每個模型使用同一數值閾值。

## 保留的原結果

- 「原始樣本（補充）」可恢復原來的 2024 名單、指標與曲線；其 rSI-sMS 與其他分科模型的分母可能不同。
- 2025 沿用既有公開封存結果，未重新讀取、計算或對齊。2025 模式停用此次樣本切換。
- SHAP／特徵重要性沿用原本已完成的 2024 解釋樣本，不代表本次共同評估樣本全體的特徵重要性。
- 既有消融、敏感度、基準模型補充及報告維持原分析樣本，未悄悄換成新的分母。
- 模型、訓練流程、模型選擇、輸出機率及原始已接受證據不變。本次沒有模型訓練或推論。

外科 P1 在共同樣本中只有 2 個事件。呈現數值及信賴區間不等於排名可靠，也不代表差異達統計顯著；結果適用範圍是共同可評分子集。

## 下載與驗證

- [共同樣本指標 CSV（324 格）](common-row-2024.csv)
- [更新的曲線、信賴區間與來源校驗碼](common-row-2024.json)
- [獨立驗證摘要](common-row-validation.json)

公開檔案僅含彙總結果、曲線與整份來源的校驗碼，不含病人 row hash、病人群組識別或逐筆預測。
